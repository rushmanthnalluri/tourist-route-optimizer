[project]
name = "tourist-planner"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []
[tool.pytest.ini_options]
python_files = ["test_*.py", "benchmark_*.py"]

[tool.pyright]
extraPaths = ["backend"]
